Parent: [[+ git]]

```shell
# Force push, but only if it's safe to do so
# See https://youtu.be/aolI_Rz0ZqY?si=Zf7NeiJsMpC6ENL7&t=1062
# This is lazygit's default when force pushing
$ git push --force-with-lease

# GPG sign a push
$ git push --signed
```
